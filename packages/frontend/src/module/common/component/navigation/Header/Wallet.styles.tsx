import { Row } from "@peersyst/react-components";
import styled from "styled-components";

export const WalletRoot = styled(Row).attrs({ position: "fixed" })`
    display: flex;
    padding-right: 10rem;
`;
