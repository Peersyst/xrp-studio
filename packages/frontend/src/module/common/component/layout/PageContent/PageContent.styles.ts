import styled from "styled-components";

export const PageContentRoot = styled.div`
    max-width: var(--page-max-width);
    margin: 0 auto;
    padding: 5rem var(--horizontal-page-padding) 3rem;
`;
