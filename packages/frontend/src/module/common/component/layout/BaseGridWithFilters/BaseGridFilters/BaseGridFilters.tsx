import { Row } from "@peersyst/react-components";
import Button from "module/common/component/input/Button/Button";

export const BaseGridFilters = (): JSX.Element => {
    return (
        <Row>
            <Button>Search and filters</Button>
        </Row>
    );
};
