import { Row, Switch, Typography } from "@peersyst/react-components";

const SwitchFilter = (): JSX.Element => {
    return (
        <Row justifyContent="space-between" gap="1.5rem">
            <Typography variant="body1" singleLine light>
                Okay Birds Contetema aldk lknadlkm admlamdlaksm
            </Typography>
            <Switch />
        </Row>
    );
};

export default SwitchFilter;
