import { css } from "styled-components";

export const ProgressBarStyles = css(() => {
    return css`
        .ProgressBar {
            height: 6px;
            border-radius: 3px;
            overflow: hidden;
        }
    `;
});
