import { css } from "styled-components";

export const ProgressBarStyles = css(() => {
    return css`
        .ProgressBar {
            height: 0.375rem;
            border-radius: 0.1875rem;
            overflow: hidden;
        }
    `;
});
